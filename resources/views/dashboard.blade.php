@extends(config('sentinel.views.layout','sentinel::layout'))
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">Moderation Dashboard</h1><p class="text-muted mb-0">Reports, cases, enforcement and risk at a glance.</p></div></div>
<div class="row g-3 mb-4">@foreach($metrics as $label=>$value)<div class="col-6 col-md-4 col-xl-2"><div class="card shadow-sm border-0"><div class="card-body"><div class="text-muted small text-uppercase">{{ str_replace('_',' ',$label) }}</div><div class="display-6 fw-semibold">{{ $value }}</div></div></div></div>@endforeach</div>
<div class="row g-4"><div class="col-xl-7"><div class="card shadow-sm border-0"><div class="card-header bg-transparent fw-semibold">Recent Cases</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Case</th><th>Priority</th><th>Status</th><th>Queue</th></tr></thead><tbody>@forelse($cases as $case)<tr><td><a href="{{ route('sentinel.cases.show',$case) }}">{{ $case->title }}</a></td><td>{{ $case->priority->value }}</td><td>{{ $case->status->value }}</td><td>{{ $case->queue }}</td></tr>@empty<tr><td colspan="4" class="text-muted">No cases.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="col-xl-5"><div class="card shadow-sm border-0"><div class="card-header bg-transparent fw-semibold">Recent Reports</div><div class="list-group list-group-flush">@forelse($reports as $report)<a class="list-group-item list-group-item-action" href="{{ route('sentinel.reports.show',$report) }}"><div class="d-flex justify-content-between"><strong>{{ $report->reason ?: $report->category }}</strong><span class="badge text-bg-secondary">{{ $report->priority->value }}</span></div><small class="text-muted">{{ $report->status->value }} · {{ $report->created_at->diffForHumans() }}</small></a>@empty<div class="list-group-item text-muted">No reports.</div>@endforelse</div></div></div></div>
@endsection
