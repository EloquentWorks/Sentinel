@extends(config('sentinel.views.layout','sentinel::layout'))
@section('content')<h1 class="h3 mb-4">Cases</h1><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Title</th><th>Priority</th><th>Status</th><th>Queue</th><th>Due</th></tr></thead><tbody>@forelse($cases as $case)<tr><td><a href="{{ route('sentinel.cases.show',$case) }}">{{ $case->title }}</a></td><td>{{ $case->priority->value }}</td><td>{{ $case->status->value }}</td><td>{{ $case->queue }}</td><td>{{ $case->due_at?->diffForHumans() ?: '—' }}</td></tr>@empty<tr><td colspan="5" class="text-muted">No cases.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $cases->links() }}</div>@endsection
