@extends(config('sentinel.views.layout','sentinel::layout'))
@section('content')<div class="d-flex justify-content-between"><div><h1 class="h3">{{ $report->reason ?: 'Moderation Report' }}</h1><p class="text-muted">{{ $report->uuid }}</p></div><span class="badge text-bg-secondary align-self-start">{{ $report->priority->value }}</span></div>
<div class="row g-4"><div class="col-lg-8"><div class="card border-0 shadow-sm"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-3">Category</dt><dd class="col-sm-9">{{ $report->category }}</dd><dt class="col-sm-3">Status</dt><dd class="col-sm-9">{{ $report->status->value }}</dd><dt class="col-sm-3">Description</dt><dd class="col-sm-9">{{ $report->description ?: '—' }}</dd><dt class="col-sm-3">Subject</dt><dd class="col-sm-9">{{ $report->subject ? class_basename($report->subject).' #'.$report->subject->getKey() : '—' }}</dd></dl></div></div></div>
<div class="col-lg-4"><div class="card border-0 shadow-sm"><div class="card-body"><form method="POST" action="{{ route('sentinel.reports.triage',$report) }}" class="mb-3">@csrf<select name="priority" class="form-select mb-2">@foreach(['low','normal','high','urgent','critical'] as $p)<option value="{{ $p }}">{{ ucfirst($p) }}</option>@endforeach</select><button class="btn btn-primary w-100">Triage</button></form><form method="POST" action="{{ route('sentinel.reports.case',$report) }}" class="mb-3">@csrf<button class="btn btn-outline-primary w-100">Open Case</button></form><form method="POST" action="{{ route('sentinel.reports.dismiss',$report) }}">@csrf<input name="reason" class="form-control mb-2" placeholder="Dismissal reason"><button class="btn btn-outline-danger w-100">Dismiss</button></form></div></div></div></div>@endsection
